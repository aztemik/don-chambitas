# S1-T13 · Interfaces de repositorio y fuente de datos falsa

| | |
|---|---|
| **Sprint** | 1 |
| **Prioridad** | 450 |
| **Responsable nominal** | GRI |
| **Estado** | pendiente |
| **Depende de** | S1-T07 |

## Objetivo

Dejar toda la capa de datos funcionando en memoria, para que las pantallas de
los sprints siguientes se desarrollen sin esperar a ninguna implementación
real.

## Contexto

Esta es la tarea que sostiene toda la capa de datos hasta que las
implementaciones reales lleguen en su turno. Si sale bien, cada una de esas
cinco tareas (S2-T07, S2-T14, S3-T09, S4-T09, S5-T07) solo cambia un enlace de
Hilt y nada más.

El backend ya está decidido: Supabase (DEC-16). Eso **no** cambia nada de esta
tarea, y sobre todo **no** te autoriza a escribir código que hable con
Supabase aquí.

## Documentos a leer

- `docs/tecnico/ARQUITECTURA.md`
- `docs/tecnico/CONTRATOS-API.md`

## Archivos a crear o modificar

```
crear: dominio/repositorio/RepositorioAuth.kt
crear: dominio/repositorio/RepositorioUsuario.kt
crear: dominio/repositorio/RepositorioTrabajador.kt
crear: dominio/repositorio/RepositorioServicios.kt
crear: dominio/repositorio/RepositorioSolicitudes.kt
crear: dominio/repositorio/RepositorioPostulaciones.kt
crear: dominio/repositorio/RepositorioChat.kt
crear: dominio/repositorio/RepositorioResenas.kt
crear: dominio/repositorio/RepositorioCatalogos.kt
crear: dominio/repositorio/RepositorioIa.kt
crear: datos/falso/FuenteDatosFalsa.kt
crear: datos/falso/*Falso.kt  (una por repositorio)
modificar: di/ModuloRepositorios.kt
```

## Qué hacer

Una interfaz por repositorio, con los métodos que se deducen de
`CONTRATOS-API.md`. Todos los métodos son `suspend` y devuelven `Resultado<T>`.

`FuenteDatosFalsa` es un singleton con listas mutables en memoria y datos de
ejemplo: 8 trabajadores con perfil y servicios, 5 solicitudes en distintos
estados, 3 conversaciones con mensajes, reseñas repartidas, y los catálogos de
categorías, estados y municipios tomados de `04_datos_semilla.sql`.

Cada implementación falsa mete un retraso de 300 ms para que los estados de
carga se puedan probar de verdad, y ofrece una forma de simular error para
probar `EstadoError`.

`ModuloRepositorios` enlaza las diez interfaces con sus implementaciones falsas.
Ese archivo es el único que cambia el día que cada implementación real entre.

## Criterios de aceptación

- [ ] Las diez interfaces existen en `dominio/repositorio/`
- [ ] Todos los métodos devuelven `Resultado<T>` y son `suspend`
- [ ] Ninguna interfaz de dominio importa nada de `datos` ni de `android.*`
- [ ] Las diez implementaciones falsas existen y devuelven datos coherentes
- [ ] Los datos de ejemplo respetan las restricciones del esquema: no hay
      reseñas de solicitudes abiertas ni postulaciones a solicitudes cerradas
- [ ] Cada falsa aplica el retraso de 300 ms
- [ ] Se puede forzar un error para probar `EstadoError`
- [ ] Cambiar de falsa a real requiere tocar **solo** `ModuloRepositorios.kt`

## Cómo probarlo

Escribe una prueba unitaria por repositorio que pida datos y verifique que
llegan. `./gradlew testDebugUnitTest` debe pasar.

## Fuera de alcance

Nada de implementaciones reales, nada de `supabase-kt`, nada de DTO. Todo eso
llega con S2-T07, S2-T14, S3-T09, S4-T09 y S5-T07, cada una en su turno.
